# Supabase project (see the ReEnvision backend). Both are safe to expose in a
# client build — access is controlled by RLS, not by secrecy of these values.
VITE_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=YOUR-ANON-OR-PUBLISHABLE-KEY
